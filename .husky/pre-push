#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running linting..."
pnpm lint || exit 1

echo "🧪 Running all tests..."
pnpm test:api || exit 1
pnpm test:web || exit 1

echo "✅ All checks passed! Pushing to GitHub..."
