flowchart LR subgraph Client U["🖥️ Browser"] end

    subgraph Edge["AWS Edge"]
        CF["☁️ CloudFront"]
    end

    subgraph Compute["AWS Compute"]
        S3W[("🪣 S3<br/>Web Assets")]
        APIGW["🔌 API Gateway"]
        LAM["λ Lambda<br/>Functions"]
    end

    subgraph AI["AWS AI/ML"]
        TX["📄 Textract"]
        BR["🧠 Bedrock"]
    end

    subgraph Data["AWS Data"]
        S3D[("🪣 S3<br/>Documents")]
        DDB[("📊 DynamoDB")]
        OS[("🔍 OpenSearch")]
    end

    subgraph Auth["AWS Security"]
        COG["🔐 Cognito"]
    end

    U --> CF
    CF --> S3W
    CF --> APIGW
    APIGW --> COG
    APIGW --> LAM
    LAM --> TX
    LAM --> BR
    LAM --> S3D
    LAM --> DDB
    LAM --> OS
